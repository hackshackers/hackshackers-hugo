version: 0.1
frontend:
  phases:
    preBuild:
      commands:
        - npm install
        - npm run index-prod
    build:
      commands:
        - hugo
  artifacts:
    baseDirectory: /public
    files:
      - '**/*'
  cache:
    paths:
      - node_modules/**/*
